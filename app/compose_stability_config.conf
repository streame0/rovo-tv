// ============================================================================
// Rovo Compose Compiler Stability Configuration
// ============================================================================
// Classes listed here are treated as @Stable by the Compose compiler.
// This eliminates unnecessary recomposition for data classes with List<>/Map<>
// fields that are effectively immutable (never mutated after construction).
// ============================================================================

// --- Stremio data models (network DTOs, never mutated after deserialization) ---
com.rovo.app.data.model.stremio.*

// --- Domain models (contain List<MetaItem> or List<HubItem>) ---
com.rovo.app.domain.*

// --- Room entities ---
com.rovo.app.data.model.WatchHistoryEntity
com.rovo.app.data.model.ProfileEntity
com.rovo.app.data.model.AddonEntity
com.rovo.app.data.model.CatalogConfigEntity
com.rovo.app.data.model.ThemeEntity
com.rovo.app.data.model.HubRowEntity
com.rovo.app.data.model.HubRowItemEntity
com.rovo.app.data.model.HubRowWithItems

// --- ViewModel state ---
com.rovo.app.ui.home.HomeViewModel.HomeState

// --- Player models ---
com.rovo.app.ui.player.base.*

// --- Profile snapshot ---
com.rovo.app.data.profile.ProfileRuntimeSnapshot

// --- Kotlin stdlib collections (List, Map, Set params treated as stable) ---
kotlin.collections.*
java.util.Collections.*

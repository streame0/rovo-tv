<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext traffic globally because Stremio addons
         use arbitrary HTTP URLs that cannot be whitelisted. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
